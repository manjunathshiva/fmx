"""fmx — a friendly CLI for Apple's on-device Foundation Model.

Brings the macOS 27 `fm` experience (chat / respond / schema) to macOS 26 today,
built on Apple's `apple-fm-sdk` (on-device only).
"""

__version__ = "0.1.0"
